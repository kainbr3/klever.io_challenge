@model kleverchallenge.Models.CryptoEntity;
@{
    ViewData["Title"] = "Delete Crypto Page";
}

<div class="text-center">

    <h1 class="display-4">Delete Crypto</h1>

    <p>TThis page uses a gRPC service</p>

    <span>@Model?.Id?</span>
    <span>@Model?.Name?</span>
    <span>@Model?.Votes?</span>

</div>